# these variables have to be defined in the environment
# and are not configurable via the server but only via
# direct (terminal) access

HARDWARE_LOCKFILE_PATH=""

HERMES_MQTT_IDENTIFIER="..."
HERMES_MQTT_URL="..."
HERMES_MQTT_PORT=8883
HERMES_MQTT_USERNAME="........"
HERMES_MQTT_PASSWORD="........"
HERMES_MQTT_BASE_TOPIC=""